filters:
  and:
    - file.ext == "md"
views:
  - type: cards
    name: Table
    filters:
      and:
        - file.folder.startsWith("posts")
    sort:
      - property: date
        direction: DESC
    image: note.image
    cardSize: 260
    imageAspectRatio: 0.55
